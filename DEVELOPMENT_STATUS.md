# حالة تطوير منصة فزعة FAZAAH

آخر تحديث: 2026-09-18
المستودع: `absherbalsafar-debug/Fazaa-app-manua`
الفرع الرئيسي: `main`
آخر commit: `0c3c70a` — إصلاح استمرار رمز التحقق أثناء إكمال الملف

## ما تم إنجازه

- نقل التطبيق إلى مستودع مستقل باسم `Fazaa-app-manua`.
- تشغيل الموقع على استضافة WebDev دائمة مع قاعدة بيانات إنتاجية.
- تسجيل المستخدم عبر رقم الهاتف ورمز OTP داخل نفس الشاشة.
- حفظ المستخدمين، رموز التحقق، والجلسات في قاعدة بيانات إنتاجية.
- إصلاح تدفق التسجيل بحيث يبقى OTP صالحاً بعد التحقق الأولي حتى يكتمل ملف المستخدم.
- شاشة إكمال ملف العميل بعنوان «أكمل بياناتك».
- إلزام العميل بإدخال الاسم الرباعي، أربعة أسماء على الأقل.
- إلزام العميل بتحديد الموقع الجغرافي قبل المتابعة.
- إضافة GPS وخريطة OpenStreetMap مع إمكانية اختيار موقع أدق.
- حفظ الدولة والمحافظة والمدينة والحي وخط الطول والعرض.
- منع إنشاء حساب العميل إذا كان الاسم أو الموقع ناقصاً.
- إضافة اختبارات لتدفق OTP والتحقق من الاسم والموقع.
- نجاح `pnpm check` و`pnpm test` و`pnpm build`.

## ما تم إنجازه في المرحلة الحالية

- إضافة جدول طلبات اعتماد المهنيين بحالات: قيد المراجعة، معتمد، مرفوض.
- إضافة جدول مستندات الاعتماد وربطه بطلب المهني.
- إضافة مسار قراءة حالة الاعتماد للمستخدم المهني المصادق عليه.
- إضافة مسار تجهيز رابط رفع آمن للمستندات مع التحقق من النوع والحجم.
- إضافة مسار حفظ بيانات المستند بعد رفعه إلى التخزين الدائم.
- حماية المسارات بجلسة الهاتف ومنع العملاء من رفع أو قراءة مستندات المهنيين.
- نجاح `pnpm check` و`pnpm test` و`pnpm build` بعد التعديل.

## قيد التنفيذ حالياً

### تسجيل واعتماد المهني

الخطوة التالية هي تحويل تسجيل المهني إلى نموذج اعتماد متعدد المراحل يشمل:

1. الاسم الرباعي.
2. الرقم الوطني والتحقق من 11 رقماً.
3. اختيار المجال الرئيسي والفرعي.
4. وصف التخصص من 50 إلى 1000 حرف.
5. الموقع الجغرافي والخريطة.
6. رقم واتساب يمني من 9 أرقام.
7. رفع الوجه الأمامي والخلفي للبطاقة.
8. رفع إثبات الخبرة اختيارياً.
9. قراءة التعهد والموافقة عليه.
10. إرسال طلب الاعتماد.

## المتبقي بعد واجهة المهني

- إضافة جداول اعتماد المهنيين والمستندات إلى قاعدة البيانات.
- ربط رفع المستندات بالتخزين الدائم.
- إنشاء حالة الحساب: قيد المراجعة، معتمد، مرفوض.
- منع ظهور المهني للعملاء قبل الاعتماد والاشتراك.
- بناء لوحة الإدارة لمراجعة طلبات الاعتماد.
- إضافة الباقات الشهرية والسنوية وربط الدفع لاحقاً.
- إضافة اختبارات تكامل للتسجيل والاعتماد والرفع.

## طريقة متابعة العمل

قبل أي تعديل، راجع هذا الملف و`README.md`. بعد كل مرحلة مهمة:

1. شغّل `pnpm check`.
2. شغّل `pnpm test`.
3. شغّل `pnpm build`.
4. حدّث هذا الملف.
5. ارفع التغييرات إلى فرع `main` مع رسالة commit واضحة.

## ملاحظات الوصول

- الوصول إلى GitHub يتيح قراءة الكود والـ commits وسجل التطوير.
- الوصول إلى GitHub وحده لا يمنح الوصول إلى أسرار WebDev أو قاعدة البيانات أو إعدادات الاستضافة.
- لمتابعة المعاينة وقاعدة البيانات، يجب منح الحساب صلاحية على مشروع WebDev `fazaa-permanent` أيضاً.
- لا توجد مفاتيح أو أسرار إنتاجية داخل المستودع.
