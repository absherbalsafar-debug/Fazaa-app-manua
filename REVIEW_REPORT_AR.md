# تقرير مراجعة مشروع فزعة FAZAAH

**المستودع:** `absherbalsafar-debug/Fazaa-app-manua`  
**الفرع المراجع:** `main`  
**نطاق المراجعة:** بنية المشروع، مصادقة رقم الجوال ورمز OTP، الجلسات، الاختبارات، واستعداد التطبيق لربط SMS الحقيقي.

## الخلاصة التنفيذية

المشروع في حالة جيدة من ناحية التنظيم العام، ويحتوي على واجهة عربية، خادم Express، قاعدة PostgreSQL عبر Drizzle/Neon، اختبارات لمصادقة الهاتف، وغلاف Android عبر Capacitor. تدفق التسجيل الحالي يعمل كما وصفتم: يتم إنشاء رمز OTP، تخزين قيمة hash في قاعدة البيانات، وإظهار الرمز داخل الشاشة مؤقتًا بدل إرساله عبر SMS.

التدفق مناسب للتطوير والاختبار، لكنه **لا ينبغي أن ينتقل إلى الإنتاج بصورته الحالية**؛ لأن الخادم يعيد الرمز في استجابة API دون تقييد واضح ببيئة التطوير، ولأن توليد الرمز يعتمد على `Math.random()`، ولا توجد حماية كافية من كثرة طلبات إعادة الإرسال. هذه نقاط سهلة المعالجة قبل ربط مزود الرسائل.

## نتائج التحقق

| الفحص | النتيجة | الملاحظة |
|---|---|---|
| `pnpm check` | ناجح | لا توجد أخطاء TypeScript حاليًا |
| `pnpm build` | ناجح | تم بناء الواجهة والخادم بنجاح |
| الاختبارات المحلية | ناجحة | 7 اختبارات ناجحة، وتشمل تدفق OTP والتسجيل العميل/المهني |
| اختبارات Neon التكاملية | لم تكتمل | تحتاج `NEON_DATABASE_URL` غير المتاح في بيئة المراجعة |

ظهرت أيضًا ملاحظة من pnpm بأن إعدادات `patchedDependencies` و`overrides` داخل حقل `pnpm` في `package.json` لم تعد تُقرأ في إصدار pnpm المستخدم. يجب نقلها لاحقًا إلى `pnpm-workspace.yaml` أو الصيغة الحديثة حتى لا تتغير التبعيات بصمت.

## ما تم تنفيذه بشكل جيد

1. **تطبيع رقم الهاتف** يدعم الإدخال المحلي وبعض الصيغ التي تبدأ بـ `00` أو `+`.
2. **تخزين رمز OTP على شكل hash** بدل تخزين الرمز نصيًا في قاعدة البيانات.
3. **صلاحية الرمز محددة بعشر دقائق**.
4. **حد المحاولات خمس محاولات** قبل طلب رمز جديد.
5. **حذف الرمز بعد نجاح التحقق** في المسار الطبيعي.
6. **تخزين الجلسات في قاعدة البيانات** مع مدة صلاحية طويلة نسبيًا.
7. وجود اختبارات تغطي التسجيل الجديد، التسجيل المكرر، العميل، المهني، الاسم الرباعي، الموقع، ونوع الحساب.
8. وجود فصل واضح نسبيًا بين مصادقة الهاتف، كتالوج المهنيين، اعتماد المهني، الاشتراكات، والواجهة.

## الملاحظات ذات الأولوية العالية

### 1. إظهار OTP غير مقيد ببيئة التطوير

في `server/phoneAuth.ts` يتم إرسال `otp: code` في استجابة `/api/auth/send-otp` دائمًا. وفي الواجهة يتم عرض القيمة داخل بطاقة بعنوان «رمز التأكيد».

هذا مناسب للاختبار، لكن إذا وصل نفس البناء أو نفس إعداد الخادم إلى الإنتاج، يستطيع أي عميل استدعاء API والحصول على رمز الدخول دون امتلاك الهاتف فعليًا.

**الإجراء المقترح:** جعل إظهار الرمز خلف متغير بيئي صريح مثل:

```text
PHONE_AUTH_SHOW_OTP=true
```

ويُسمح به فقط عندما تكون البيئة تطويرية أو تجريبية. أما الإنتاج فيرجع فقط:

```json
{
  "success": true,
  "phone": "+967...",
  "expiresInSeconds": 600
}
```

كما ينبغي ألا تعتمد الواجهة على وجود `data.otp`، بل تعرض بطاقة الرمز فقط إذا كان الخادم قد أعادها في وضع تجريبي.

### 2. استخدام `Math.random()` لتوليد رمز أمني

الكود الحالي يستخدم:

```ts
Math.floor(100000 + Math.random() * 900000)
```

لرمز الدخول. هذا غير مناسب لرمز مصادقة حقيقي.

**الإجراء المقترح:** استخدام `randomInt(100000, 1000000)` من `node:crypto`، ثم مقارنة الـ hash بطريقة آمنة زمنيًا باستخدام `timingSafeEqual` بعد تحويل القيم إلى buffers.

### 3. عدم وجود معدل طلبات لإرسال OTP

يمكن حاليًا استدعاء `/api/auth/send-otp` عدة مرات، وكل طلب يستبدل الرمز السابق. عند تشغيل SMS الحقيقي سيؤدي ذلك إلى:

- تكلفة رسائل غير ضرورية.
- إزعاج المستخدم.
- احتمال إساءة استخدام الخدمة.
- جعل الرمز السابق غير صالح بسرعة.

**الإجراء المقترح:** إضافة فترة انتظار لإعادة الإرسال، مثل 60 ثانية لكل رقم، وحد يومي/ساعي لكل رقم ولكل عنوان IP. يجب إرجاع `retryAfterSeconds` للواجهة حتى تعرض عدادًا واضحًا.

### 4. غياب حماية عامة من إساءة استخدام مسارات المصادقة

حد المحاولات موجود لكل رمز، لكنه لا يعالج كثرة طلبات الإرسال أو المحاولات على أرقام متعددة من نفس المصدر.

**الإجراء المقترح:** إضافة rate limiting على مساري `send-otp` و`verify-otp`، مع سجل محاولات مختصر أو جدول مخصص إذا كان التطبيق سيعمل على أكثر من نسخة خادم. الاعتماد على `Map` داخل الذاكرة وحده لا يكفي في بيئة موزعة أو بعد إعادة التشغيل.

### 5. الجلسة محفوظة في `localStorage`

الواجهة تحفظ `fazaah_token` في `localStorage`. هذا عملي للتطوير، لكنه يكون معرضًا للسرقة إذا وُجد ثغرة XSS.

**الإجراء المقترح للمرحلة الإنتاجية:** نقل الجلسة إلى Cookie من نوع `HttpOnly`, `Secure`, `SameSite=Lax` أو `SameSite=Strict` حسب التدفق، مع حماية CSRF مناسبة. ويمكن إبقاء التخزين المحلي مؤقتًا إذا كان هناك سبب تقني في نسخة Android، لكن يجب توثيق المخاطر.

### 6. عدم التحقق من حالة المستخدم أثناء `/api/auth/me`

المسار يتحقق من وجود الجلسة والمستخدم، لكنه لا يظهر أنه يمنع المستخدم الموقوف (`suspended`) من الاستمرار في استعمال الجلسة.

**الإجراء المقترح:** رفض الجلسة أو تحديث الحالة عند قراءة المستخدم إذا كانت حالته `suspended`، مع حذف/إبطال جلساته عند إيقاف الحساب.

## ملاحظات وظيفية مهمة

### وجود مسارين مختلفين لتسجيل الدخول

توجد شاشة `auth-phone.tsx` الخاصة بتسجيل الدخول عبر OTP، وفي المقابل توجد شاشة `login.tsx` قديمة تعتمد رقم الهاتف وكلمة المرور وتستخدم `useLogin`. هذا قد يسبب ارتباكًا للمستخدم أو مسارًا لا يتوافق مع النظام الحالي.

**التوصية:** اعتماد OTP كمسار الدخول الرئيسي، ثم إما إزالة مسار كلمة المرور أو إبقاؤه مؤقتًا خلف قرار واضح. يجب أن تقود روابط «تسجيل الدخول» و«تسجيل» إلى نفس التدفق حسب `mode=login` و`mode=register`.

### إكمال ملف العميل بعد التحقق الأولي

التدفق يحافظ على OTP حتى يتم إرسال بيانات الاسم والموقع، وهذا يعالج المشكلة التي تم توثيقها سابقًا. يُفضّل إضافة اختبار صريح للتأكد من أن الرمز لا يُستهلك عند الرد `needsRegistration: true`، ثم يُستهلك فقط بعد إنشاء الحساب بنجاح.

### إدخال السنوات والحقول الرقمية

يتم تحويل بعض القيم من جسم الطلب باستخدام `Number()`، لكن يلزم إضافة حدود منطقية، خصوصًا `yearsExperience` و`categoryId`، مع منع القيم السالبة أو الكبيرة جدًا.

### التحقق من المدخلات

المسارات تعتمد حاليًا على فحوص يدوية كثيرة. من الأفضل تعريف مخططات Zod مشتركة للطلبين `send-otp` و`verify-otp` حتى تكون الرسائل والقواعد موحدة بين الخادم والواجهة، ويقل خطر قبول بيانات غير متوقعة.

## خطة التطوير المقترحة

### المرحلة الأولى: تأمين وضع OTP الحالي

1. إضافة `PHONE_AUTH_SHOW_OTP` مع منع تفعيله في الإنتاج.
2. استبدال `Math.random` بمولد آمن من `node:crypto`.
3. إضافة فترة إعادة إرسال ومحددات rate limiting.
4. إضافة اختبارات للتأكد من عدم ظهور OTP عند تعطيل المتغير.
5. إضافة اختبارات للمحاولات السادسة وإعادة الإرسال المبكر.
6. توحيد رسائل وأحوال `login` و`register`.

### المرحلة الثانية: ربط مزود SMS

1. إنشاء واجهة `SmsProvider` مستقلة عن منطق المصادقة.
2. تنفيذ مزود حقيقي عبر مفاتيح بيئية سرية، دون وضع الأسرار في GitHub.
3. جعل الاختبارات تستخدم مزودًا وهميًا `FakeSmsProvider`.
4. تسجيل نتيجة الإرسال دون تسجيل الرمز نفسه في السجلات.
5. إضافة معالجة فشل المزود وإعادة المحاولة بحذر.

### المرحلة الثالثة: تحسين الجلسات والهوية

1. نقل التوكن إلى Cookie آمن أو تقييم الحل المناسب لغلاف Android.
2. إبطال الجلسات القديمة عند تغيير الحالة أو تسجيل الخروج من جميع الأجهزة.
3. إضافة تنظيف دوري للجلسات والرموز المنتهية.
4. إضافة سجل أمني مبسط لعمليات الدخول والفشل.

### المرحلة الرابعة: توحيد تجربة التطبيق

1. توحيد شاشة `login.tsx` مع `auth-phone.tsx`.
2. تحسين حالات التحميل والمهلة وإعادة الإرسال.
3. إضافة إدخال OTP مخصص مع انتقال تلقائي بين الخانات ودعم لصق الرمز.
4. إضافة رسائل واضحة عند انتهاء الصلاحية أو بلوغ حد المحاولات.
5. اختبار مسارات Android والعودة للخلف وحفظ حالة التسجيل.

### المرحلة الخامسة: إكمال دورة اعتماد المهني

1. إنهاء رفع مستندات الهوية والخبرة.
2. منع ظهور المهني للعملاء قبل الاعتماد والاشتراك، مع اختبار تكاملي لذلك.
3. استكمال لوحة الإدارة مع صلاحيات إدارية صريحة.
4. إضافة إشعارات حالة طلب الاعتماد.
5. تأجيل ربط الدفع الحقيقي إلى مرحلة منفصلة بعد تثبيت دورة الاعتماد.

## التوصية العملية التالية

أوصي بالبدء مباشرة بالمرحلة الأولى فقط، مع الإبقاء على إظهار الرمز في بيئة التطوير عبر متغير بيئي. بعد نجاح `check` والاختبارات المحلية واختبارات التكامل على Neon، ننتقل إلى اختيار مزود SMS وتنفيذ واجهة قابلة للتبديل.

لا أوصي حاليًا بتفعيل SMS الحقيقي قبل إغلاق النقاط الخمس الأولى، وخصوصًا تقييد إظهار OTP، استخدام مولد عشوائي آمن، وإضافة حماية إعادة الإرسال.

## الملفات الرئيسية التي تمت مراجعتها

- `server/phoneAuth.ts`
- `server/phoneAuth.test.ts`
- `client/src/pages/auth-phone.tsx`
- `client/src/lib/auth.tsx`
- `client/src/pages/login.tsx`
- `drizzle/schema.ts`
- `server/_core/index.ts`
- `DEVELOPMENT_STATUS.md`

